engine-strict=true
# npm 7+ enforces peer dependencies strictly, which this legacy Gatsby 2
# dependency tree cannot satisfy. npm 6 (used by the Netlify build via
# .node-version) ignores this setting.
legacy-peer-deps=true
